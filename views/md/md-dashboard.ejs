<%- include('md-header', { pageTitle: 'MD Dashboard' }) %>

        <div class="content-header">
          <div class="page-title">MD Dashboard</div>
          <form method="POST" action="/md_logout" style="display: inline;">
            <button type="submit" class="logout-btn">Logout</button>
          </form>
        </div>

        <div class="dashboard-content">
          <div class="welcome-section">
            <h2>Welcome to MD (Analytics) Dashboard</h2>
            <p>Select an option from the sidebar to manage your analytics and reports.</p>
          </div>

          <div class="dashboard-grid" style="margin-top: 20px; display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: 16px;">
            <div class="dashboard-card">
              <div class="card-title">👥 Total Candidates</div>
              <div class="card-content">
                <p style="font-size: 28px; font-weight: 700;"><%= typeof totalCandidates !== 'undefined' ? totalCandidates : 0 %></p>
              </div>
            </div>
          </div>

<%- include('md-footer') %>
